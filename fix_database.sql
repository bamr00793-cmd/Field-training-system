-- ============================================================
-- شغّلي هذا الملف مرة وحدة فقط على قاعدة field_training_system
-- من phpMyAdmin -> اختاري القاعدة -> تبويب SQL -> الصقي و نفذي
-- ============================================================

-- 1) سبب عدم عمل زر "تفعيل الحساب" و"إلغاء التفعيل":
--    عمود status غير موجود أصلاً بجدول users
ALTER TABLE `users`
ADD COLUMN `status` ENUM('Active','Pending') NOT NULL DEFAULT 'Pending';

-- 2) سبب عدم تحديث "حالة تقييم الطالب" (وأيضاً فقدان نص التوصيات فعلياً):
--    عمود recommendation معرّف كـ ENUM بقيم محددة، بينما نموذج
--    التقييم (evaluate_student.php) يرسل نص حر عبر textarea لا يطابق
--    أي من هذه القيم، فتُخزَّن التوصية كنص فارغ '' في كل مرة.
--    الحل: تحويل العمود إلى نص حر (TEXT) ليحفظ التوصية فعلياً.
ALTER TABLE `evaluations`
MODIFY COLUMN `recommendation` TEXT NULL DEFAULT NULL;

-- ملاحظة: هذا لن يمسح أي بيانات موجودة، فقط يوسّع نوع العمود.
-- بعد تنفيذ الأمرين أعلاه، لا حاجة لأي تعديل إضافي على قاعدة البيانات.
